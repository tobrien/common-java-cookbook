** Aggregate Statistics

Average: $stat.average( $results.scores )%
Lowest Score: $stat.min( $results.scores )%
Highest Score: $stat.max( $results.scores )%

** Scores:
#foreach( $student in $results.students )
	#score( $student 50 )
#end

More results are available here:
http://www.tests.com/detail?testId=${results.id}